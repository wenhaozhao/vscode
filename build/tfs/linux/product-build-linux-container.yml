resources:
  containers:
  - container: vscode-x64
    image: joaomoreno/vscode-linux-build-agent:x64

pool:
  vmImage: 'Ubuntu-16.04'

variables:
  VSCODE_ARCH: x64

container: vscode-x64

steps:
- script: |
    echo hello world
